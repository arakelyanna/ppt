#pragma once
#include <iostream>
#include <string>
#include "Shape.h"

namespace obj{
    class Slide{
    public:
        Slide() = default;
    private:
        size_t position;
        std::vector<IO>
    };
}